{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install && ng build --prod"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

